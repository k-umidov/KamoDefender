# API Документация - KamoDefender

## Обзор

KamoDefender использует несколько внешних API для улучшения функциональности:

### 1. Have I Been Pwned API (HIBP)
**Статус:** ✅ Активно  
**Стоимость:** Бесплатно  
**Назначение:** Проверка паролей на наличие в базах данных утечек

#### Особенности:
- **k-Anonymity модель** - отправляется только первые 5 символов SHA-1 хеша пароля
- **Приватность** - полный пароль никогда не отправляется на сервер
- **База данных** - более 11+ миллиардов скомпрометированных паролей
- **Без регистрации** - не требует API ключа

#### Использование:
```javascript
const result = await window.SecurityAPI.checkPasswordBreach('MyPassword123');
console.log(result);
// {
//   breached: true,
//   count: 12345,
//   message: "⚠️ ВНИМАНИЕ! Этот пароль найден в 12,345 утечках данных!"
// }
```

---

### 2. IP Geolocation API
**Статус:** ✅ Активно  
**Стоимость:** Бесплатно (до 1000 запросов/день)  
**Назначение:** Получение информации о местоположении домена/IP

#### Особенности:
- Определение страны и города
- Информация о провайдере
- Без регистрации

#### Использование:
```javascript
const info = await window.SecurityAPI.getIPInfo('https://google.com');
console.log(info);
// {
//   available: true,
//   country: "United States",
//   city: "Mountain View",
//   org: "Google LLC",
//   message: "📍 United States, Mountain View | Google LLC"
// }
```

---

### 3. VirusTotal API (Опционально)
**Статус:** ⚠️ Отключено по умолчанию  
**Стоимость:** Бесплатно (4 запроса/минуту), платные тарифы доступны  
**Назначение:** Проверка URL на вредоносное содержимое

#### Как активировать:

1. **Получите API ключ:**
   - Зарегистрируйтесь на [VirusTotal](https://www.virustotal.com/)
   - Перейдите в профиль → API Key
   - Скопируйте ваш ключ

2. **Настройте api.js:**
   ```javascript
   const API_CONFIG = {
       virustotal: {
           baseUrl: 'https://www.virustotal.com/api/v3/',
           enabled: true,  // ← Измените на true
           apiKey: 'ВАШ_API_КЛЮЧ_ЗДЕСЬ'  // ← Вставьте ключ
       }
   };
   ```

3. **Использование:**
   ```javascript
   const result = await window.SecurityAPI.checkURLWithVirusTotal('https://example.com');
   console.log(result);
   // {
   //   scanAvailable: true,
   //   malicious: 0,
   //   suspicious: 0,
   //   harmless: 65,
   //   message: "✅ URL не отмечен как вредоносный"
   // }
   ```

#### Лимиты:
- **Бесплатный:** 4 запроса в минуту, 500 запросов в день
- **Premium:** До 1000 запросов в минуту

---

## Безопасность и Приватность

### Защита данных пользователей:

1. **Пароли (HIBP):**
   - Используется SHA-1 хеширование на стороне клиента
   - Отправляются только первые 5 символов хеша (k-Anonymity)
   - Оригинальный пароль никогда не покидает браузер

2. **URL:**
   - Отправляется только доменное имя для геолокации
   - VirusTotal получает полный URL (если активирован)

3. **Файлы:**
   - Анализируются ТОЛЬКО локально
   - Содержимое файлов не передается на сервера

### CORS и ограничения:

Некоторые API могут быть заблокированы браузером из-за CORS политики при локальном запуске.  
**Решение:** Используйте веб-сервер (например, Live Server в VS Code)

---

## Обработка ошибок

Все API функции возвращают объекты с полем `error` при неудаче:

```javascript
const result = await window.SecurityAPI.checkPasswordBreach('test');

if (result.error) {
    console.log('Произошла ошибка:', result.message);
} else {
    console.log('Проверка успешна:', result.message);
}
```

---

## Производительность

### Оптимизация запросов:

1. **Кэширование** - результаты не кэшируются для актуальности данных
2. **Асинхронность** - все API вызовы асинхронные, не блокируют UI
3. **Таймауты** - встроенные таймауты для предотвращения зависания

### Рекомендации:

- Не проверяйте один пароль многократно подряд (HIBP может ограничить)
- VirusTotal имеет строгие лимиты - используйте разумно
- IP Geolocation лучше работает с доменами, чем с IP адресами

---

## Примеры интеграции

### Проверка пароля с полным анализом:

```javascript
async function fullPasswordCheck(password) {
    // Локальный анализ
    const localAnalysis = analyzePassword(password);
    
    // Проверка утечек
    const breachCheck = await window.SecurityAPI.checkPasswordBreach(password);
    
    return {
        ...localAnalysis,
        breach: breachCheck
    };
}
```

### Проверка URL с геолокацией:

```javascript
async function fullURLCheck(url) {
    // Локальный анализ
    const localAnalysis = analyzeURL(url);
    
    // Геолокация
    const ipInfo = await window.SecurityAPI.getIPInfo(url);
    
    return {
        ...localAnalysis,
        location: ipInfo
    };
}
```

---

## Поддержка и обновления

- API модуль: `api.js`
- Версия: 1.0.0
- Последнее обновление: 2026-02-11

### Обратная связь:
- Telegram: [@um1dov7](https://t.me/um1dov7)
- Email: umidovkamoliddin2006@gmail.com

---

## Лицензия

API интеграции используют публичные сервисы:
- HIBP API - Attribution-ShareAlike 4.0
- VirusTotal - Условия VirusTotal
- IP Geolocation - Freemium модель

**Важно:** Соблюдайте условия использования каждого API сервиса.
